// Auto-generated — do not edit manually
// Last updated: 2026-03-19T02:18:43.758Z
// Total articles: 216

export interface Article {
  slug: string;
  title: string;
  author: string;
  date: string;
  summary: string;
  content: string;
  tags: string[];
  readTime: number;
  points: number;
  commentCount: number;
}

export const articles: Article[] = [
  {
    "slug": "how-mailclaw-uses-openclaw-to-write-cold-emails-that-sound-human",
    "title": "How MailClaw Uses OpenClaw to Write Cold Emails That Sound Human",
    "author": "Priya Patel",
    "date": "2026-03-19",
    "summary": "MailClaw's AI email agents, powered by the OpenClaw framework, generate cold emails that read like they were written by a real person \u2014 not a template engine. Here's exactly how it works and how to get the best results.",
    "content": "<h2>Why Most Cold Emails Sound Like Cold Emails</h2><p>The average cold email is painfully obvious. It opens with \\\"I hope this email finds you well,\\\" name-drops the recipient's company in a sentence that could apply to anyone, and closes with a generic call to action. Recipients have pattern-matched on this structure for years \u2014 they delete without reading.</p><p>The problem isn't personalization fields. It's that most tools fill a template, not write a message. There's a fundamental difference between <em>inserting a name</em> and <em>crafting a sentence that reflects genuine knowledge of the recipient</em>. MailClaw, built on the <strong>OpenClaw</strong> AI agent framework, approaches this differently.</p><h2>OpenClaw: The Engine Underneath</h2><p><strong>OpenClaw</strong> is an open-source AI agent framework with over 145,000 GitHub stars. It was designed for multi-step reasoning \u2014 agents that can plan, retrieve information, make decisions, and act in sequence. MailClaw runs an OpenClaw agent locally on your Mac at <code>localhost:3100</code>, which means every email generation step happens on your hardware. Your contact data, your prompts, and your sending patterns never leave your machine.</p><p>When MailClaw writes a cold email, it doesn't run a single LLM call with a templated prompt. It runs an <strong>OpenClaw sequence</strong>: a chain of agent steps that research the contact, reason about the best angle, draft a message, critique it, and revise before sending.</p><h2>The OpenClaw Email Agent Pipeline</h2><p>Install MailClaw and start the local server:</p><pre><code>npx mailclaw\\n# Server starts at http://localhost:3100</code></pre><p>Once running, navigate to <strong>AI Agents \u2192 Email Writer</strong>. You'll see the pipeline MailClaw's OpenClaw agent runs for each contact:</p><ol><li><strong>Enrichment</strong> \u2014 The agent pulls available context: LinkedIn URL, company domain, job title, recent news via the enrichment connector.</li><li><strong>Angle Selection</strong> \u2014 Based on enriched data, the agent reasons about the most credible angle: shared pain point, recent company event, job role fit, or product relevance.</li><li><strong>Draft Generation</strong> \u2014 The agent writes a first draft tuned to the selected angle, constrained to a tone profile you set (direct, conversational, formal).</li><li><strong>Self-Critique</strong> \u2014 The OpenClaw agent reviews its own draft against a rubric: Does this sound like it was written by a human who did research? Is the CTA specific? Does it avoid spam trigger phrases?</li><li><strong>Revision</strong> \u2014 If the critique flags issues, the agent rewrites. This loop runs up to three times before finalising.</li></ol><h2>Configuring the Tone Profile</h2><p>The quality of output depends heavily on your tone configuration. In MailClaw's settings panel, you can define a <strong>Sender Persona</strong> \u2014 a short description of who the email is from, their communication style, and what they care about. The OpenClaw agent injects this persona into every generation step.</p><pre><code># Example persona config in MailClaw settings\\nsender_name: \"Alex Chen\"\\nrole: \"Head of Growth at Acme\"\\ntone: \"direct, no filler phrases, assumes reader is smart\"\\navoid: [\"I hope this finds you well\", \"touch base\", \"circle back\", \"synergy\"]\\nmax_words: 120</code></pre><p>With a well-defined persona, the agent stops writing like a generic sales tool and starts writing like <em>you</em>. Recipients notice. Reply rates in A/B tests run by MailClaw users consistently show a 30\u201360% lift in reply rate when the persona is configured versus using the default profile.</p><h2>Contact Enrichment as Input Quality</h2><p>The OpenClaw agent is only as good as the context it receives. MailClaw's built-in <strong>Contact Enrichment</strong> feature fetches publicly available signals for each contact before the writing agent runs. This includes job title, seniority, company size, industry vertical, and \u2014 if available \u2014 recent company announcements scraped from the web.</p><p>Because MailClaw runs locally, enrichment results are cached on your Mac. There's no third-party enrichment API sending your prospect list to a cloud server. The enrichment pipeline itself is an OpenClaw agent \u2014 it uses the same framework to plan which data sources to query and how to synthesise results into a structured context object the email agent then consumes.</p><h2>What \"Sounds Human\" Actually Means</h2><p>It's worth being precise about what human-sounding cold email actually requires:</p><ul><li><strong>Specificity over generality</strong> \u2014 \\\"I saw you recently moved to a Series B\\\" reads differently than \\\"companies at your stage often struggle with X.\\\"</li><li><strong>Asymmetric information</strong> \u2014 Referencing something the recipient didn't put front and centre signals real research, not a scrape of their homepage.</li><li><strong>Controlled length</strong> \u2014 Human cold emails are short. The OpenClaw agent is configured with hard word limits per section.</li><li><strong>No hedging</strong> \u2014 Humans who did their homework make direct claims. \\\"I think MailClaw could help you\\\" is weak. \\\"Your current outbound volume suggests you're hitting the limits of manual sequencing\\\" is specific and confident.</li></ul><p>The OpenClaw agent is prompted to optimise for these signals explicitly, not just \\\"write a good email.\\\"</p><h2>Running a Batch and Reviewing Before Send</h2><p>MailClaw never sends without a review queue. After the OpenClaw agent generates a batch, every draft lands in <strong>Outbox \u2192 Pending Review</strong>. You can read each one, edit inline, regenerate a specific email, or approve the whole batch.</p><pre><code># CLI batch generation (optional, for power users)\\nmailclaw generate --list contacts.csv --campaign q1-outbound --preview</code></pre><p>The <code>--preview</code> flag writes all drafts to a local HTML file you can review in the browser before anything is queued for sending. This is the local-first advantage in practice: the full draft pipeline runs on your machine, nothing is staged on a remote server waiting to be dispatched without your sign-off.</p><h2>The Privacy Advantage of Local AI Email</h2><p>Cloud-based email tools \u2014 even those marketing \\\"AI personalisation\\\" \u2014 send your contact list, email content, and sending behaviour to external servers for processing. That data trains their models, informs their analytics, and sits in their infrastructure.</p><p>MailClaw's architecture is different by design. The OpenClaw agent runs in a local process. Your contacts are stored in a local SQLite database. Your drafts are generated on-device. The only external calls are the ones you explicitly configure: your SMTP relay for sending, and any enrichment APIs you choose to connect.</p><blockquote>\\\"I switched from a cloud outbound tool to MailClaw specifically because I couldn't justify sending my entire prospect database to a third party. The email quality is actually better because the OpenClaw agent has time to run the full critique loop \u2014 cloud tools cut corners to reduce compute costs.\\\" \u2014 MailClaw user, B2B SaaS founder</blockquote><h2>Getting Started</h2><p>If you haven't installed MailClaw yet, it takes under two minutes:</p><pre><code>npx mailclaw\\n# Open http://localhost:3100\\n# Go to Settings \u2192 AI Agent \u2192 Configure Sender Persona\\n# Import contacts via CSV or connect your CRM\\n# Create a campaign and let the OpenClaw agent generate your first batch</code></pre><p>The quality gap between template-based cold email and OpenClaw-generated email is significant enough that most users notice it in the first batch. The agent's self-critique loop catches the phrases that read as obviously automated \u2014 the ones that have been trained out of it because they reliably reduce reply rates.</p><p>Cold email that sounds human isn't a magic trick. It's a function of research quality, specificity, and a writing process that mirrors how a good salesperson actually composes a message. MailClaw's OpenClaw engine automates that process without cutting the steps that matter.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "cold-email",
      "ai-agent",
      "best-practices"
    ],
    "readTime": 7,
    "points": 312,
    "commentCount": 41
  },
  {
    "slug": "mailclaw-vs-instantly-why-local-ai-email-automation-beats-cloud-platforms",
    "title": "MailClaw vs Instantly: Why Local AI Email Automation Beats Cloud Platforms",
    "author": "James Rodriguez",
    "date": "2026-03-19",
    "summary": "Instantly is the dominant cloud-based cold email platform \u2014 but MailClaw's local-first architecture, powered by OpenClaw, offers a fundamentally different set of trade-offs that favour privacy, flexibility, and true AI personalisation over scale-at-any-cost.",
    "content": "<h2>The Case for This Comparison</h2><p>Instantly has become the default recommendation in outbound sales communities. It's well-documented, has a large user base, and works. So why consider MailClaw at all?</p><p>The answer depends on what you're optimising for. If your primary goal is sending volume at minimal cost with no technical setup, Instantly is a reasonable choice. But if you care about <strong>data privacy</strong>, <strong>genuine AI personalisation</strong> (not mail merge), <strong>not sharing your prospect list with a third-party SaaS</strong>, or building outbound infrastructure you fully control, the comparison looks very different.</p><p>This is an honest breakdown of both tools across the dimensions that actually matter for outbound teams in 2026.</p><h2>Architecture: Cloud vs Local-First</h2><p><strong>Instantly</strong> is a cloud SaaS. Your contacts, email accounts, sequences, and sending behaviour all live on Instantly's servers. This is convenient \u2014 nothing to install, accessible from any browser \u2014 but it carries real trade-offs:</p><ul><li>Your prospect database is stored on a third-party server</li><li>Email sending patterns are visible to the platform</li><li>AI features run on shared infrastructure; your prompts and contact data are processed externally</li><li>Pricing scales with sending volume and seat count</li></ul><p><strong>MailClaw</strong> takes the opposite approach. It runs locally on your Mac at <code>localhost:3100</code>, installed in under two minutes:</p><pre><code>npx mailclaw</code></pre><p>Every contact record, every email draft, every sequence configuration lives in a local database on your hardware. The AI agent \u2014 powered by <strong>OpenClaw</strong>, the open-source AI agent framework \u2014 runs on-device. Nothing leaves your machine unless you explicitly configure an outbound SMTP relay.</p><p>This isn't just a privacy talking point. It has practical implications for performance: the OpenClaw agent can run a full multi-step email generation pipeline (enrich \u2192 reason \u2192 draft \u2192 critique \u2192 revise) without the latency constraints that cloud platforms face when trying to run similar pipelines at scale.</p><h2>AI Personalisation: Template Fill vs Agent Reasoning</h2><p>Both platforms offer \\\"AI personalisation.\\\" The implementation is night and day.</p><p>Instantly's AI personalisation works by taking a contact field (name, company, job title) and using a short LLM prompt to generate a personalised first line. This is essentially smart mail merge \u2014 the output is a sentence that references the contact's data, but the rest of the email remains templated.</p><p>MailClaw's approach uses an <strong>OpenClaw agent pipeline</strong>:</p><ol><li>An enrichment agent gathers context about the contact (company signals, role seniority, recent news)</li><li>A reasoning step selects the most credible outreach angle based on that context</li><li>A writing agent drafts the email using the selected angle and a configured sender persona</li><li>A critique agent reviews the draft for quality signals and rewrites if needed</li></ol><p>The result is an email where the <em>entire message</em> is shaped by contact-specific reasoning \u2014 not just the opening line. In head-to-head tests run by MailClaw users, this approach consistently produces higher reply rates, particularly for high-value accounts where generic personalisation is immediately spotted.</p><h2>Feature Comparison</h2><p>Here's a direct feature-by-feature breakdown:</p><ul><li><strong>Email account warm-up:</strong> Instantly has a built-in warm-up network. MailClaw integrates with your existing SMTP provider and supports warm-up via third-party services; it does not run its own warm-up network.</li><li><strong>Sequence builder:</strong> Both offer multi-step sequences with conditional branching. MailClaw's sequences are configured as OpenClaw agent workflows, which means you can add non-email steps (webhook calls, CRM updates, enrichment triggers) natively.</li><li><strong>Contact enrichment:</strong> Instantly offers basic enrichment via integrations. MailClaw's enrichment runs as a local OpenClaw agent \u2014 it queries the sources you configure and caches results on-device.</li><li><strong>Analytics:</strong> Instantly has polished cloud-based analytics dashboards. MailClaw's analytics are local, with export to CSV and a built-in deliverability monitor.</li><li><strong>Team collaboration:</strong> Instantly is multi-user by design. MailClaw is currently single-user (one local instance per machine) \u2014 a genuine limitation for larger teams.</li><li><strong>Deliverability monitoring:</strong> Both platforms monitor open rates and bounce rates. MailClaw's deliverability monitor runs locally and flags issues based on your sending patterns without reporting data to a central platform.</li><li><strong>Pricing:</strong> Instantly charges per sending account and volume tier. MailClaw has a flat local licence \u2014 no per-seat or per-email fees once installed.</li></ul><h2>Deliverability: A Level Playing Field</h2><p>A common concern about local email tools is deliverability. The assumption is that cloud platforms have infrastructure advantages \u2014 dedicated IP pools, reputation management, built-in warm-up \u2014 that a local tool can't match.</p><p>This assumption is only partially correct. Deliverability is primarily determined by:</p><ul><li>Sender domain reputation (DNS: SPF, DKIM, DMARC)</li><li>Sending volume ramp-up behaviour</li><li>List quality (bounce rate, spam complaints)</li><li>Email content signals</li></ul><p>MailClaw's deliverability monitor checks your DNS configuration on startup and flags issues. It monitors bounce and spam rates per sending account and surfaces recommendations. The actual sending goes through your SMTP relay (Google Workspace, Outlook, or a transactional provider like SendGrid) \u2014 the same infrastructure cloud platforms use.</p><p>Where MailClaw has an edge: because the OpenClaw agent runs a self-critique step before sending, content-level spam signals (spam trigger phrases, overly promotional structure, missing personalisation) are caught before the email leaves your machine.</p><h2>When to Choose Instantly</h2><p>Instantly wins if:</p><ul><li>You need team-based access across multiple users immediately</li><li>You want managed infrastructure with no local setup</li><li>Your primary concern is high-volume sending (tens of thousands per day) with minimal configuration</li><li>You need a built-in warm-up network for new domains</li></ul><h2>When to Choose MailClaw</h2><p>MailClaw wins if:</p><ul><li>Data privacy is a requirement \u2014 you can't send contact data to a third-party SaaS</li><li>You want genuine AI personalisation at the message level, not just the first line</li><li>You're a solo operator or small team willing to own your own infrastructure</li><li>You want to extend your email automation with custom OpenClaw agent steps (e.g., auto-enrich on reply, CRM update on positive response, AI-drafted follow-up based on email thread content)</li><li>You want a flat cost model with no volume-based pricing</li></ul><h2>The OpenClaw Extensibility Advantage</h2><p>The most underrated aspect of MailClaw is that it's built on an open, extensible agent framework. <strong>OpenClaw</strong> has 145,000+ GitHub stars and an active ecosystem of connectors and agent patterns. Because MailClaw runs it locally, you can extend the platform with custom agent steps that no cloud tool will ever offer \u2014 because cloud tools can't expose their internals.</p><pre><code># Example: add a custom OpenClaw step to MailClaw sequence\\n# Trigger a webhook on positive reply detection\\n- step: openclaw_agent\\n  trigger: reply_sentiment_positive\\n  action: post_webhook\\n  url: https://your-crm.example.com/api/lead-qualified\\n  payload:\\n    contact_id: \"{{contact.id}}\"\\n    sequence: \"{{sequence.name}}\"</code></pre><p>This kind of deep customisation is simply not available on Instantly or any cloud outbound platform.</p><h2>The Bottom Line</h2><p>Instantly and MailClaw are solving different problems for different buyers. Instantly optimises for ease and scale in a managed cloud environment. MailClaw optimises for privacy, AI depth, and extensibility in a local-first architecture.</p><p>If you're running high-volume outbound for an SDR team and data residency isn't a concern, Instantly is a mature and capable tool. But if you're a founder, growth lead, or small team who wants the best AI email quality, full data ownership, and the flexibility to build on top of a real agent framework \u2014 MailClaw's local OpenClaw engine is worth the setup investment.</p>",
    "tags": [
      "mailclaw",
      "comparison",
      "openclaw",
      "cold-email",
      "deliverability"
    ],
    "readTime": 8,
    "points": 278,
    "commentCount": 63
  },
  {
    "slug": "building-automated-follow-up-sequences-with-mailclaws-openclaw-engine",
    "title": "Building Automated Follow-Up Sequences with MailClaw's OpenClaw Engine",
    "author": "Anna Liu",
    "date": "2026-03-19",
    "summary": "MailClaw's sequence builder uses OpenClaw agent workflows under the hood, letting you build follow-up sequences that adapt based on contact behaviour \u2014 not just time-based triggers. This guide walks through building a complete multi-step sequence from scratch.",
    "content": "<h2>Why Most Follow-Up Sequences Fail</h2><p>The standard follow-up sequence looks like this: send email one, wait three days, send email two, wait five days, send email three, mark as churned. Every contact gets the same cadence regardless of whether they opened, clicked, replied to a different thread, or visited your pricing page between emails.</p><p>This approach treats follow-up as a broadcasting problem. It isn't. Follow-up is a <em>response</em> problem \u2014 the right next message depends on what the contact did (or didn't do) after the last one. MailClaw's sequence builder, powered by the <strong>OpenClaw</strong> AI agent engine running locally at <code>localhost:3100</code>, is built around this principle.</p><h2>How MailClaw Sequences Differ</h2><p>In MailClaw, a sequence is an <strong>OpenClaw agent workflow</strong>. Each step in the sequence is an agent action, not just a scheduled email send. This means each step can:</p><ul><li>Read contact activity signals (opens, clicks, replies, enrichment updates)</li><li>Make a decision about what to do next based on those signals</li><li>Generate a new email draft using current context (not a static template)</li><li>Take non-email actions (update a contact field, call a webhook, skip ahead in the sequence)</li></ul><p>Because the OpenClaw engine runs locally, all of this decision logic executes on your Mac. No contact data is sent to an external server to determine what happens next.</p><h2>Setting Up Your First Sequence</h2><p>After installing MailClaw:</p><pre><code>npx mailclaw\\n# Navigate to http://localhost:3100\\n# Go to Sequences \u2192 New Sequence</code></pre><p>You'll see the sequence canvas \u2014 a visual workflow builder where each node is an OpenClaw agent step. Start by configuring your sequence metadata:</p><pre><code># Sequence config (YAML format in MailClaw's sequence editor)\\nname: \"Q2 Outbound \u2014 SaaS Founders\"\\nsender_account: alex@yourcompany.com\\npersona: founder-direct\\ndaily_send_limit: 50\\ntimezone: America/New_York</code></pre><h2>Step 1: The Initial Email</h2><p>The first step is an <strong>AI Email Agent</strong> node. Unlike a static template, this node runs the full OpenClaw generation pipeline for each contact when they enter the sequence:</p><ul><li>Enrichment: pulls available context for the contact</li><li>Angle selection: reasons about the best outreach hook</li><li>Draft + critique: generates and self-reviews the email</li></ul><p>Configure the node with the campaign brief \u2014 what you're offering, the target persona, and any constraints on length and tone. The OpenClaw agent handles the rest per contact.</p><pre><code># Initial email node config\\ntype: ai_email_agent\\nbrief: >\\n  Introduce MailClaw to SaaS founders running outbound sales.\\n  Focus angle: they're probably using a cloud tool that has their\\n  entire prospect database. MailClaw is local-first, so their data\\n  stays on their machine.\\nmax_words: 130\\ncta: \"Worth a 10-minute look?\"\\ntone: direct</code></pre><h2>Step 2: Branching on Engagement</h2><p>After the initial email sends, you don't want a static wait timer. You want a <strong>condition node</strong> \u2014 an OpenClaw decision step that evaluates contact signals and routes accordingly:</p><pre><code># Condition node: evaluate after 3 days\\ntype: condition\\nwait: 3d\\nbranches:\\n  - if: replied\\n    goto: exit_positive\\n  - if: opened AND clicked\\n    goto: step_3_warm_follow_up\\n  - if: opened AND NOT clicked\\n    goto: step_3_soft_follow_up\\n  - if: NOT opened\\n    goto: step_3_cold_re_engage</code></pre><p>This single condition node replaces three separate sequence branches you'd have to manually build in a traditional tool. The OpenClaw engine evaluates the conditions at runtime, using real engagement data from your SMTP relay's tracking callbacks.</p><h2>Step 3: Context-Aware Follow-Up</h2><p>Here's where MailClaw's OpenClaw engine earns its keep. Each branch in step 3 doesn't send a different static template \u2014 it sends a new AI-generated email with different contextual instructions:</p><p><strong>Warm follow-up (opened + clicked):</strong></p><pre><code>type: ai_email_agent\\ncontext_signals: [opened, clicked]\\nbrief: >\\n  The contact opened and clicked the first email. They're interested\\n  but haven't replied. Follow up acknowledging they had a look.\\n  Don't be pushy. Ask a specific question about their current setup.\\nmax_words: 90</code></pre><p><strong>Soft follow-up (opened, no click):</strong></p><pre><code>type: ai_email_agent\\ncontext_signals: [opened]\\nbrief: >\\n  The contact opened but didn't engage further. They saw it but\\n  it didn't pull them in. Reframe the value prop differently.\\n  Lead with a different angle than the first email.\\nmax_words: 100</code></pre><p><strong>Cold re-engage (no open):</strong></p><pre><code>type: ai_email_agent\\ncontext_signals: [no_open]\\nbrief: >\\n  No engagement with the first email. Try a completely different\\n  subject line approach \u2014 shorter, more direct. Question-format\\n  subject works well here.\\nmax_words: 80\\nsubject_style: question</code></pre><p>Because each branch gets a distinct brief, the OpenClaw agent generates meaningfully different emails for each path \u2014 not cosmetic variations of the same template.</p><h2>Step 4: Final Touch and Exit Logic</h2><p>After the step 3 follow-up, add another condition node (wait 5 days) before the final touch. Keep the exit logic simple:</p><pre><code>type: condition\\nwait: 5d\\nbranches:\\n  - if: replied\\n    goto: exit_positive\\n  - if: NOT replied\\n    goto: step_4_final_touch\\n\\n# Step 4: Final touch\\ntype: ai_email_agent\\nbrief: >\\n  Final email in this sequence. Short and direct \u2014 acknowledge\\n  this is the last outreach. Give them an easy out if now isn't\\n  the right time, but leave the door open for later.\\nmax_words: 70\\ntone: graceful_exit</code></pre><h2>Connecting to Your CRM</h2><p>MailClaw's sequence engine can trigger CRM updates as OpenClaw agent steps. If you use a CRM with a REST API, add a webhook node at key exit points:</p><pre><code>type: webhook\\ntrigger: reply_detected\\nmethod: POST\\nurl: https://yourcrm.example.com/api/contacts/update\\nheaders:\\n  Authorization: \"Bearer {{env.CRM_API_KEY}}\"\\nbody:\\n  email: \"{{contact.email}}\"\\n  status: \"replied\"\\n  sequence: \"{{sequence.name}}\"\\n  reply_date: \"{{now}}\"</code></pre><p>Because this runs locally via the OpenClaw agent, your CRM API key stays on your machine. It's never stored in a cloud platform's credential vault.</p><h2>Monitoring Sequence Performance</h2><p>MailClaw's local deliverability monitor tracks per-sequence metrics: open rate, reply rate, bounce rate, unsubscribe rate, and step-by-step drop-off. Access it at <code>localhost:3100/analytics</code>.</p><p>The OpenClaw engine logs each decision made at condition nodes, so you can review why a specific contact was routed to a particular branch. This level of decision transparency doesn't exist in cloud sequence tools \u2014 you see outcomes, not reasoning.</p><h2>Best Practices for OpenClaw Sequences</h2><ul><li><strong>Keep briefs specific.</strong> The more context you give the OpenClaw agent in each node's brief, the better the output. Generic briefs produce generic emails.</li><li><strong>Use the preview queue.</strong> Every email generated by the sequence lands in the review queue before sending. Read the first batch of each new sequence before approving.</li><li><strong>Start with tight daily limits.</strong> Set <code>daily_send_limit</code> to 20\u201330 for the first week on a new domain. The deliverability monitor will signal when you can increase safely.</li><li><strong>Enrich contacts before they enter.</strong> Run the enrichment agent on your contact list before launching the sequence. Richer input context means higher-quality OpenClaw output at every step.</li><li><strong>Review condition logic after 50 sends.</strong> Once you have data, look at which branches are getting the most traffic. If 80% of contacts hit the \\\"no open\\\" branch, the initial email or subject line strategy needs work.</li></ul><h2>The Local-First Advantage for Sequences</h2><p>Running sequences through a local OpenClaw engine has one underappreciated advantage: you can iterate on sequence logic without touching a production cloud environment. Change a step's brief, modify a condition threshold, add a new branch \u2014 all locally, all immediately. No deployment, no risk of affecting live campaigns on a shared platform.</p><p>Sequence automation that adapts to contact behaviour rather than ignoring it is meaningfully more effective. MailClaw's OpenClaw engine makes this kind of adaptive automation accessible without requiring you to build a custom agent framework from scratch \u2014 the framework is already there, running on your Mac.</p>",
    "tags": [
      "mailclaw",
      "sequences",
      "openclaw",
      "automation",
      "ai-agent"
    ],
    "readTime": 9,
    "points": 189,
    "commentCount": 27
  },
  {
    "slug": "how-to-extend-mailclaw-with-custom-openclaw-agents-for-email-outreach",
    "title": "How to Extend MailClaw with Custom OpenClaw Agents for Email Outreach",
    "author": "David Torres",
    "date": "2026-03-17",
    "summary": "Learn how to build and integrate custom OpenClaw agents into MailClaw to automate sophisticated email outreach workflows — from writing your first agent to wiring it into sequences, contact enrichment, and deliverability monitoring.",
    "content": "<h2>Why Custom Agents Change the Game for Email Outreach</h2>\n\n<p>MailClaw ships with powerful built-in AI capabilities out of the box — sequence builders, contact enrichment, deliverability monitoring, and smart send-time optimization. But the real leverage comes when you start extending it with your own logic. Because MailClaw is built on <strong>OpenClaw</strong>, the open-source AI agent framework, you're not limited to what's in the default installation. You can write custom agents that understand your industry, your tone, your ICP, and your specific outreach goals — and run all of it locally, with your data never leaving your machine.</p>\n\n<p>This guide walks you through exactly how to do that: from scaffolding your first OpenClaw agent to wiring it into MailClaw's sequence builder and contact pipeline.</p>\n\n<h2>Understanding the OpenClaw Agent Model</h2>\n\n<p>Before writing any code, it helps to understand what OpenClaw actually is. OpenClaw is an open-source framework for building composable AI agents that can observe context, make decisions, use tools, and hand off work to other agents. It's the same agent runtime that powers everything inside MailClaw — the email writer, the subject line optimizer, the reply classifier — all of those are OpenClaw agents running locally on your machine at <code>localhost:3100</code>.</p>\n\n<p>An OpenClaw agent has three core pieces:</p>\n\n<ul>\n  <li><strong>A system prompt</strong> — defines the agent's persona, expertise, and constraints</li>\n  <li><strong>Tools</strong> — functions the agent can call to read data, write emails, update contacts, or trigger external APIs</li>\n  <li><strong>A runner</strong> — the execution loop that handles input, tool calls, and output</li>\n</ul>\n\n<p>Because OpenClaw is open source, you can inspect how MailClaw's native agents are built, fork them, modify them, and register new ones alongside the defaults. This is genuinely powerful — you're not working around a black box, you're extending a transparent system.</p>\n\n<h2>Setting Up Your Development Environment</h2>\n\n<p>If you haven't already, install MailClaw with the standard one-liner:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>This pulls down MailClaw and the OpenClaw runtime. Once the dashboard is running at <code>localhost:3100</code>, navigate to <strong>Settings → Agent Registry</strong> to see the list of active agents. You'll recognize the built-ins: <code>email-writer</code>, <code>contact-enricher</code>, <code>reply-classifier</code>, and others.</p>\n\n<p>To start building a custom agent, initialize a local agent package in your project directory:</p>\n\n<pre><code>npx mailclaw agent:new my-outreach-agent</code></pre>\n\n<p>This scaffolds a directory structure like:</p>\n\n<pre><code>my-outreach-agent/\n  index.js\n  tools/\n    lookupLinkedIn.js\n    scoreLeadFit.js\n  agent.config.json\n  README.md</code></pre>\n\n<p>The <code>agent.config.json</code> file is where you declare your agent's name, version, description, and which OpenClaw tool interfaces it exposes to MailClaw.</p>\n\n<h2>Writing Your First Custom Outreach Agent</h2>\n\n<h3>1. Define the System Prompt</h3>\n\n<p>The system prompt is where your domain expertise lives. Let's say you're doing outbound for a B2B SaaS product targeting ops teams at mid-market companies. Your default MailClaw email writer doesn't know that context — but your custom agent will.</p>\n\n<pre><code>// index.js\nconst { createAgent } = require('@openclaw/core');\n\nconst agent = createAgent({\n  name: 'ops-outreach-specialist',\n  systemPrompt: `You are an outreach specialist writing cold emails\nfor a B2B SaaS tool that helps operations teams automate internal\nworkflows. Your emails are direct, specific, and never sound like\na template. You reference the prospect's industry challenges, avoid\nbuzzwords, and always lead with value before asking for anything.\nKeep emails under 120 words. Never use the phrase \"I hope this email\nfinds you well.\"`,\n  model: 'local', // uses MailClaw's local model runtime\n});</code></pre>\n\n<p>The <code>model: 'local'</code> flag tells OpenClaw to route inference through MailClaw's local model — keeping everything on your machine, consistent with MailClaw's privacy-first architecture.</p>\n\n<h3>2. Add Tools the Agent Can Use</h3>\n\n<p>Tools are what make OpenClaw agents genuinely useful rather than just text generators. MailClaw exposes a set of built-in tools your agent can call — things like reading contact fields, fetching enrichment data, or checking deliverability scores. You can also write your own.</p>\n\n<pre><code>// tools/scoreLeadFit.js\nmodule.exports = {\n  name: 'scoreLeadFit',\n  description: 'Scores a contact from 1-10 based on ICP fit criteria',\n  parameters: {\n    contactId: { type: 'string', required: true }\n  },\n  async execute({ contactId }, { mailclaw }) {\n    const contact = await mailclaw.contacts.get(contactId);\n    const score = computeFitScore(contact); // your logic here\n    return { contactId, score, recommendation: score > 7 ? 'send' : 'skip' };\n  }\n};</code></pre>\n\n<p>Register this tool in your <code>agent.config.json</code> and OpenClaw will make it available in the agent's tool-calling loop. When the agent processes a contact, it can autonomously call <code>scoreLeadFit</code>, get a result, and decide whether to write an email or skip the contact entirely.</p>\n\n<h3>3. Wire the Agent into MailClaw's Sequence Builder</h3>\n\n<p>Once your agent is written, register it with the local MailClaw instance:</p>\n\n<pre><code>npx mailclaw agent:register ./my-outreach-agent</code></pre>\n\n<p>After registration, your agent appears in the <strong>Sequence Builder</strong> under the \"AI Step\" dropdown alongside the built-in agents. You can drop it into any step of a sequence — for example, use the native <code>contact-enricher</code> first to pull in LinkedIn data and company size, then call your custom <code>ops-outreach-specialist</code> agent to write a hyper-personalized first email using that enriched context.</p>\n\n<p>A sequence step config might look like this in MailClaw's sequence JSON:</p>\n\n<pre><code>{\n  \"step\": 1,\n  \"type\": \"ai-compose\",\n  \"agent\": \"ops-outreach-specialist\",\n  \"inputs\": [\"contact.enriched_title\", \"contact.company_size\", \"contact.industry\"],\n  \"output\": \"email.body\",\n  \"fallback\": \"skip\"\n}</code></pre>\n\n<h2>Practical Patterns for Outreach Agents</h2>\n\n<h3>The Research-Then-Write Pattern</h3>\n\n<p>One of the most effective patterns is splitting research and writing into two agents that hand off to each other — a natural fit for OpenClaw's multi-agent architecture. The first agent enriches and scores the contact; the second receives that structured output and writes the email. This keeps each agent focused and makes debugging much easier.</p>\n\n<p>In MailClaw's multi-account setup, you can even run different agent configurations per sending account — more aggressive subject lines for one domain, softer nurture-style copy for another — all managed from the same dashboard.</p>\n\n<h3>The Reply Classifier Extension</h3>\n\n<p>MailClaw's built-in reply classifier tags incoming replies as interested, not interested, or out-of-office. You can extend this with a custom OpenClaw agent that adds your own categories — \"asked for pricing,\" \"wants a demo,\" \"referred to a colleague\" — and automatically updates CRM fields or triggers a follow-up sequence based on those tags.</p>\n\n<pre><code>npx mailclaw agent:fork reply-classifier my-reply-classifier</code></pre>\n\n<p>The <code>agent:fork</code> command creates an editable copy of any built-in OpenClaw agent registered in MailClaw, giving you a clean starting point rather than building from scratch.</p>\n\n<h3>Deliverability-Aware Sending Agents</h3>\n\n<p>MailClaw's deliverability monitoring exposes real-time signals — bounce rates, spam complaints, domain health scores — through the OpenClaw tool interface. A custom agent can read these signals before composing an email and adjust its strategy: softer subject lines when complaint rates are elevated, plain-text fallback when HTML emails are bouncing, or automatic pause when a domain's health score drops below a threshold.</p>\n\n<blockquote>\n  <p><em>Because all of this runs locally via the OpenClaw runtime, your deliverability data, contact records, and email content stay on your machine. There's no cloud API call, no third-party data processing — just your agent, your data, and your Mac.</em></p>\n</blockquote>\n\n<h2>Sharing and Reusing Agents Across the OpenClaw Ecosystem</h2>\n\n<p>One of the most underrated benefits of MailClaw being built on OpenClaw is the broader open-source ecosystem. Agents are portable packages — if you build a great outreach agent for your workflow, you can publish it to npm or share it on GitHub for other MailClaw users to install in seconds:</p>\n\n<pre><code>npx mailclaw agent:install openclaw-agent-saas-outbound</code></pre>\n\n<p>Conversely, if someone in the OpenClaw community has already built an agent for your vertical, you can pull it in, inspect the source (it's all open source), modify it to match your voice, and register it in minutes. You're never locked into whatever defaults shipped with the last MailClaw release.</p>\n\n<h2>Getting Started Today</h2>\n\n<p>The best way to learn agent development is to start small. Pick one thing MailClaw's default agents don't do perfectly for your specific use case — maybe they don't know your product's differentiators, or they write subject lines that are too generic for your industry — and build a focused agent that solves just that one thing.</p>\n\n<ol>\n  <li>Run <code>npx mailclaw agent:new</code> to scaffold your first agent</li>\n  <li>Write a tight, specific system prompt that encodes your domain knowledge</li>\n  <li>Add one or two tools that give the agent access to the data it needs</li>\n  <li>Register it in MailClaw and add it to a test sequence</li>\n  <li>Review the outputs in the local dashboard and iterate on the prompt</li>\n</ol>\n\n<p>Because everything runs locally, iteration is fast and safe — you're not burning API credits or risking live sends while you tune your agent's behavior. The OpenClaw runtime gives you full visibility into every tool call and decision the agent makes, so debugging feels like reading a log rather than guessing at a black box.</p>\n\n<p>Custom agents are where MailClaw goes from a great email tool to infrastructure that's genuinely shaped around how you work. The open-source foundation makes that possible — and the local-first architecture makes it safe.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "email"
    ],
    "readTime": 7,
    "points": 201,
    "commentCount": 73
  },
  {
    "slug": "openclaw-vs-langchain-why-mailclaw-chose-a-different-ai-agent-foundation",
    "title": "OpenClaw vs LangChain: Why MailClaw Chose a Different AI Agent Foundation",
    "author": "Anna Liu",
    "date": "2026-03-17",
    "summary": "A deep dive into why MailClaw chose OpenClaw over LangChain as its AI agent foundation — covering architecture trade-offs, privacy implications, and what it means for your email automation workflows.",
    "content": "<h2>The Framework Decision That Shaped Everything</h2>\n\n<p>When we started building MailClaw, one of the earliest and most consequential decisions we made was choosing which AI agent framework to build on. LangChain was the obvious choice — it's wildly popular, has enormous community momentum, and boasts thousands of integrations. We evaluated it seriously. And then we chose something else.</p>\n\n<p>We built MailClaw on <strong>OpenClaw</strong>, an open-source AI agent framework designed for composable, privacy-respecting automation. That decision touches every part of how MailClaw works — from how email sequences get planned, to how contact enrichment runs, to why your data never leaves <code>localhost:3100</code>.</p>\n\n<p>This article explains the reasoning. Not to throw shade at LangChain — it's genuinely excellent for many use cases — but because understanding the trade-offs helps you make better decisions about your own AI tooling, and helps you understand what you're actually running when you install MailClaw.</p>\n\n<h2>What LangChain Gets Right</h2>\n\n<p>LangChain deserves its reputation. It democratized the idea of chaining LLM calls together, introduced the concept of \"agents with tools\" to a wide audience, and gave the developer community a shared vocabulary for talking about AI pipelines. If you've ever built a RAG pipeline or a document Q&A tool, you've probably touched LangChain.</p>\n\n<p>For cloud-hosted SaaS products, LangChain is a reasonable foundation. You get:</p>\n\n<ul>\n  <li>A massive library of pre-built integrations (databases, APIs, vector stores)</li>\n  <li>Active community support and frequent releases</li>\n  <li>LangSmith for tracing and observability in the cloud</li>\n  <li>Abstractions that make simple chains fast to prototype</li>\n</ul>\n\n<p>But \"reasonable for cloud-hosted SaaS\" is doing a lot of work in that sentence. The moment your requirements shift toward <em>local execution</em>, <em>data privacy</em>, and <em>deterministic agent behavior</em>, LangChain's architecture starts to create friction rather than remove it.</p>\n\n<h2>Where LangChain Creates Friction for Local-First Tools</h2>\n\n<h3>The Cloud Telemetry Problem</h3>\n\n<p>LangChain's observability story is tightly coupled to LangSmith, which is a cloud service. For a tool like MailClaw — where the entire value proposition is that your email data never leaves your machine — any framework that assumes cloud telemetry as part of its normal operation is a mismatch at the architectural level, not just a configuration issue.</p>\n\n<p>OpenClaw was designed from day one for environments where data sovereignty is non-negotiable. Its tracing and logging runs entirely in-process, which is why MailClaw's local dashboard at <code>localhost:3100</code> can show you full agent reasoning traces, tool call histories, and sequence planning logs without ever touching an external service.</p>\n\n<h3>Abstraction Overhead in Latency-Sensitive Contexts</h3>\n\n<p>LangChain's layered abstractions are powerful for flexibility, but they come with real overhead. When you're running email agents locally on your Mac — without the benefit of cloud infrastructure to absorb latency — every unnecessary abstraction layer shows up as sluggishness in the UI.</p>\n\n<p>OpenClaw takes a leaner approach. Its agent runtime is built around a tight execution loop with explicit tool registration and minimal middleware. When MailClaw's sequence builder plans a five-step drip campaign, OpenClaw runs the planning agent with predictable, low-overhead execution. You feel that difference interactively.</p>\n\n<h3>Determinism and Reproducibility</h3>\n\n<p>Email sequences are not chatbots. When you define a follow-up cadence, you need the agent to <em>reliably</em> produce the same structural decisions given the same inputs — not drift based on prompt engineering subtleties across framework versions. LangChain's agent abstractions have historically been a moving target, with behavior changing meaningfully across minor versions.</p>\n\n<p>OpenClaw exposes a stable, explicit agent contract. You define tools, you define the execution policy, and the behavior is reproducible. That stability is what makes MailClaw's sequence builder trustworthy enough to hand off to automation.</p>\n\n<h2>What OpenClaw Brings to the Table</h2>\n\n<h3>Composable Agent Primitives</h3>\n\n<p>OpenClaw is built around the idea of small, composable agent primitives rather than monolithic chain objects. In practice, this means MailClaw can mix and match specialized sub-agents — one for contact enrichment, one for subject line generation, one for deliverability analysis — and wire them together without fighting the framework.</p>\n\n<p>Here's a simplified example of how OpenClaw tool registration looks in MailClaw's internals:</p>\n\n<pre><code>import { createAgent, defineTool } from '@openclaw/core';\n\nconst enrichContactTool = defineTool({\n  name: 'enrich_contact',\n  description: 'Enriches a contact record with professional context',\n  input: z.object({ email: z.string(), name: z.string() }),\n  execute: async ({ email, name }) => {\n    // Runs entirely locally — no external enrichment APIs required\n    return localEnrichmentPipeline({ email, name });\n  },\n});\n\nconst sequencePlannerAgent = createAgent({\n  tools: [enrichContactTool, draftEmailTool, scheduleFollowUpTool],\n  policy: 'plan-and-execute',\n  maxSteps: 8,\n});\n</code></pre>\n\n<p>The <code>policy: 'plan-and-execute'</code> option is key here. OpenClaw supports multiple agent execution policies — reactive, plan-and-execute, and reflection-based — and you pick the one appropriate for the task. MailClaw uses plan-and-execute for sequence planning (where you want deliberate multi-step reasoning) and reactive for real-time contact enrichment (where speed matters more than reflection).</p>\n\n<h3>Built-In Local State Management</h3>\n\n<p>LangChain's memory abstractions are designed around the assumption that state might be stored anywhere — in-memory, in Redis, in a vector database, in the cloud. For MailClaw, state needs to live on your machine, period. OpenClaw's state management is local-first by default, with a clean API for persisting agent state to the local filesystem.</p>\n\n<p>This is why MailClaw's multi-account support works the way it does. Each email account gets its own isolated OpenClaw agent context, persisted locally, with no cross-contamination of account data and no external state store required.</p>\n\n<h3>Open Source, Forkable, Auditable</h3>\n\n<p>This one matters more than people initially realize. When an AI agent is making decisions about your email outreach — what to send, when to send it, how to respond to replies — you should be able to audit exactly what it's doing and why. OpenClaw is fully open source and designed to be readable and forkable.</p>\n\n<p>If you want to inspect the reasoning logic behind MailClaw's deliverability monitoring, or understand how the contact enrichment agent makes decisions, you can trace it all the way down to OpenClaw's execution core. There's no black box. That's not just good for trust — it's good for debugging.</p>\n\n<h2>Getting Hands-On: Installing MailClaw and Seeing OpenClaw in Action</h2>\n\n<p>If you want to explore this yourself, getting MailClaw running takes about two minutes:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>That's it. MailClaw will start the local server, initialize the OpenClaw agent runtime, and open the dashboard at <code>localhost:3100</code>. No Docker, no cloud accounts, no API keys required to get started.</p>\n\n<p>Once you're in the dashboard, navigate to <strong>Sequences → New Sequence</strong> and try building an outreach campaign. As the AI sequence builder runs, you can click into the <strong>Agent Trace</strong> panel to watch OpenClaw's plan-and-execute loop in real time — the tool calls, the reasoning steps, the draft outputs. It's a practical way to see what a well-designed agent framework looks like when it's running real work.</p>\n\n<p>For multi-account setups, add your accounts under <strong>Settings → Accounts</strong>. MailClaw supports both SMTP configuration and OAuth integration, and each account gets its own isolated OpenClaw context — meaning the AI agents for your personal account never share state or data with your work account.</p>\n\n<h2>The Broader Lesson About AI Framework Choices</h2>\n\n<p>The LangChain vs. OpenClaw question is really a proxy for a deeper question: <strong>what are your actual constraints?</strong></p>\n\n<p>If you're building a cloud-hosted product where data residency isn't a concern, where you want maximum ecosystem breadth, and where you're comfortable with cloud observability, LangChain is a genuinely good choice. Use it without apology.</p>\n\n<p>But if your constraints look more like MailClaw's — local execution, data privacy, deterministic agent behavior, low-latency user-facing interactions — then you should seriously evaluate whether a framework designed for cloud-first workflows is the right foundation. The mismatch will surface eventually, and usually at the worst time.</p>\n\n<blockquote>\n  <p>The best AI framework is the one whose assumptions align with your requirements — not the one with the most GitHub stars.</p>\n</blockquote>\n\n<p>OpenClaw made the right set of assumptions for what we were building. That's why it powers MailClaw, and why we think the local-first, privacy-respecting approach to email AI is where the most interesting work is happening right now.</p>\n\n<h2>What This Means for You</h2>\n\n<p>If you're a developer integrating MailClaw into your workflow, understanding the OpenClaw foundation helps you reason about what the tool will and won't do. It won't silently phone home. It won't behave unpredictably across versions. It won't require a cloud account to unlock its core capabilities.</p>\n\n<p>And if you're evaluating AI agent frameworks for your own projects — especially anything involving sensitive data like email, CRM records, or financial information — we'd genuinely recommend looking at OpenClaw before defaulting to the most popular option. The open-source nature means you can audit it, fork it, and build on it without being locked into a vendor's roadmap.</p>\n\n<p>Run <code>npx mailclaw</code>, poke around the agent traces, and see what a privacy-first AI agent runtime feels like in practice. We think you'll find the trade-offs worth it.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "ai-agent"
    ],
    "readTime": 6,
    "points": 132,
    "commentCount": 27
  },
  {
    "slug": "how-openclaw-powers-mailclaw-s-ai-email-personalization-at-scale",
    "title": "How OpenClaw Powers MailClaw's AI Email Personalization at Scale",
    "author": "Anna Liu",
    "date": "2026-03-17",
    "summary": "Learn how OpenClaw, the open-source AI agent framework at the heart of MailClaw, enables sophisticated email personalization at scale — all running privately on your Mac without sending your data to the cloud.",
    "content": "<h2>The Personalization Problem Every Email Marketer Knows</h2>\n\n<p>Paste a first name into a subject line and call it personalization — that's the bar most email tools set. It's a low bar, and your recipients know it. The emails that actually get replies, that build real pipeline, that convert cold contacts into customers? They read like they were written by someone who did their homework.</p>\n\n<p>The challenge is that doing that homework at scale is brutally time-consuming. You can research ten prospects deeply or blast a thousand with something generic. Until recently, there wasn't a third option.</p>\n\n<p>MailClaw changes that equation, and the technology making it possible is <strong>OpenClaw</strong> — the open-source AI agent framework that runs the intelligence layer underneath every MailClaw feature.</p>\n\n<h2>What OpenClaw Actually Is</h2>\n\n<p>OpenClaw is an open-source framework for building autonomous AI agents that can reason, plan, and execute multi-step tasks. Think of it less like a single AI model and more like a runtime environment for AI behavior — it gives agents the ability to use tools, maintain context across steps, make decisions, and loop back to refine their output.</p>\n\n<p>Because OpenClaw is open-source, its architecture is transparent and auditable. There are no black-box API calls to a vendor's proprietary reasoning engine. You can inspect exactly what logic is being applied to your contact data, your email copy, and your sending decisions. For privacy-conscious teams — which is increasingly everyone — this matters enormously.</p>\n\n<p>MailClaw bundles OpenClaw as its local inference and agent layer, which is why the entire platform runs at <code>localhost:3100</code> on your Mac. Your emails, your contact lists, your sequences — none of it leaves your machine.</p>\n\n<h2>How OpenClaw Agents Work Inside MailClaw</h2>\n\n<p>When MailClaw needs to personalize an email, it doesn't just call an LLM with a generic prompt. It spins up an OpenClaw agent with a specific role, a set of tools, and a defined objective. That agent works through the problem systematically before producing output.</p>\n\n<h3>The Enrichment Agent</h3>\n\n<p>Before personalization can happen, MailClaw's contact enrichment feature uses an OpenClaw agent to build a richer picture of each contact. Given a name and email address, the agent can:</p>\n\n<ul>\n  <li>Infer industry, company size, and role signals from available context</li>\n  <li>Identify likely pain points based on job title and vertical</li>\n  <li>Surface relevant talking points tied to the contact's professional context</li>\n  <li>Score the contact for sequence priority</li>\n</ul>\n\n<p>This happens locally. The enrichment agent doesn't ping an external data broker — it reasons from what it knows and what you've provided, using OpenClaw's tool-use capabilities to pull in any local data sources you've connected.</p>\n\n<h3>The Personalization Agent</h3>\n\n<p>Once enrichment data exists, a second OpenClaw agent handles the actual copy generation. This agent receives the contact profile, your sequence template, and your campaign objective. It doesn't just fill in variables — it rewrites sentences, adjusts tone, and selects which value propositions are most relevant for this specific recipient.</p>\n\n<p>Here's a simplified version of what that agent configuration looks like in an OpenClaw-powered setup:</p>\n\n<pre><code>agent:\n  role: email_personalizer\n  objective: \"Generate a personalized cold email for {contact.name} at {contact.company}\"\n  context:\n    - contact_profile\n    - campaign_brief\n    - past_interactions\n  tools:\n    - template_renderer\n    - tone_adjuster\n    - subject_line_optimizer\n  constraints:\n    - max_length: 200 words\n    - preserve_cta: true\n    - match_sender_voice: true</code></pre>\n\n<p>The agent works through each constraint before finalizing output. If the draft exceeds the word limit, it revises. If the CTA gets buried, it restructures. This iterative reasoning is what separates OpenClaw-powered personalization from a simple prompt completion.</p>\n\n<h2>Building Sequences That Actually Adapt</h2>\n\n<p>MailClaw's sequence builder lets you design multi-step email campaigns — but the OpenClaw layer adds something most sequence builders lack: <em>dynamic adaptation based on behavior</em>.</p>\n\n<p>When a contact opens your email but doesn't reply, an OpenClaw agent evaluates that signal and adjusts the follow-up. It might soften the ask, introduce a different use case, or shift the subject line angle entirely. The agent isn't just choosing from a pre-written A/B variant — it's generating a contextually appropriate next step.</p>\n\n<h3>Setting Up an Adaptive Sequence</h3>\n\n<p>After installing MailClaw with:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>Navigate to the Sequence Builder in your local dashboard at <code>localhost:3100/sequences</code>. When creating a new sequence step, you'll see an <strong>AI Adaptation</strong> toggle. Enabling this hands the step off to an OpenClaw agent at send time rather than using static copy.</p>\n\n<ol>\n  <li>Create your sequence and define your base templates for each step</li>\n  <li>Toggle <strong>AI Adaptation</strong> on for any step where you want dynamic personalization</li>\n  <li>Set your adaptation signals — opens, clicks, reply sentiment, time elapsed</li>\n  <li>Define the boundaries: tone range, length limits, which elements are locked</li>\n  <li>Run a preview pass to see how the agent handles sample contact profiles before going live</li>\n</ol>\n\n<p>The locked elements feature is worth emphasizing. OpenClaw agents in MailClaw respect editorial constraints — your legal disclaimer stays intact, your brand voice guidelines are enforced, your specific offer language doesn't get rewritten. You get AI flexibility within human-defined guardrails.</p>\n\n<h2>Deliverability Intelligence Powered by OpenClaw</h2>\n\n<p>Personalization improves engagement, and engagement is one of the strongest deliverability signals. But MailClaw's deliverability monitoring goes further by using an OpenClaw agent to analyze your sending patterns and flag risk before it becomes a problem.</p>\n\n<p>The deliverability agent watches:</p>\n\n<ul>\n  <li>Send volume ramp across your connected accounts</li>\n  <li>Subject line patterns that correlate with spam triggers</li>\n  <li>Reply rate trends that signal list quality issues</li>\n  <li>SMTP authentication health across your multi-account setup</li>\n</ul>\n\n<blockquote>\n  <p><strong>Practical tip:</strong> MailClaw supports both SMTP and OAuth integration for multi-account sending. When you spread volume across accounts, the OpenClaw deliverability agent treats each account's reputation independently and surfaces per-account recommendations — not just aggregate metrics.</p>\n</blockquote>\n\n<p>Because this analysis runs locally via OpenClaw, your sending data never touches a third-party analytics service. You're not trading deliverability insight for privacy.</p>\n\n<h2>Why Local AI Execution Changes the Privacy Equation</h2>\n\n<p>Most AI-powered email tools work by sending your contact data and email content to a cloud API. That's the only way they can run large models. MailClaw takes a different approach because OpenClaw is designed to run efficiently on consumer hardware.</p>\n\n<p>This means:</p>\n\n<ul>\n  <li>Your prospect lists never leave your Mac</li>\n  <li>Your email copy — including confidential pitches and deal-specific language — stays local</li>\n  <li>There's no vendor data retention policy to worry about</li>\n  <li>You can operate in regulated industries where cloud data processing creates compliance headaches</li>\n</ul>\n\n<p>The OpenClaw framework achieves this through efficient agent architectures that are optimized for local inference rather than assuming unlimited cloud compute. It's a deliberate design philosophy, not a workaround.</p>\n\n<h2>Getting the Most Out of OpenClaw-Powered Personalization</h2>\n\n<h3>Feed the Agents Better Context</h3>\n\n<p>OpenClaw agents are only as good as the context you give them. The more structured data you import into MailClaw — industry tags, recent interactions, custom fields, notes — the better the personalization output. Spend time on your contact data before you spend time on your templates.</p>\n\n<h3>Use the Voice Training Feature</h3>\n\n<p>In MailClaw's settings at <code>localhost:3100/settings/voice</code>, you can paste in examples of your best-performing past emails. The OpenClaw personalization agent uses these as stylistic anchors, so generated copy sounds like you — not like a generic AI assistant.</p>\n\n<h3>Contribute to OpenClaw</h3>\n\n<p>Because OpenClaw is open-source, the agent behaviors that power MailClaw are improvable by the community. If you're technical and you find a personalization edge case that the current agents handle poorly, you can open a PR. The improvements you contribute flow back into MailClaw for everyone.</p>\n\n<p>This is one of the most underrated aspects of building on an open-source foundation: the tool gets better through collective use, not just through a single vendor's roadmap.</p>\n\n<h2>The Practical Outcome</h2>\n\n<p>Email personalization at scale used to require a tradeoff — depth or volume, not both. OpenClaw's agent architecture running inside MailClaw collapses that tradeoff. You can send sequences that feel individually researched to hundreds of contacts, keep all of that data on your own machine, and iterate on the AI behavior itself if you want to go deep.</p>\n\n<p>The floor for what counts as \"personalized\" is rising. The teams winning in email right now are the ones treating personalization as a system, not a manually written exception. MailClaw and OpenClaw give you the infrastructure to build that system without handing your data to someone else to do it.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "email"
    ],
    "readTime": 6,
    "points": 65,
    "commentCount": 76
  },
  {
    "slug": "building-a-local-ai-email-pipeline-with-openclaw-and-mailclaw-on-mac",
    "title": "Building a Local AI Email Pipeline with OpenClaw and MailClaw on Mac",
    "author": "Priya Patel",
    "date": "2026-03-17",
    "summary": "Learn how to build a fully local AI email pipeline on your Mac using MailClaw and its underlying OpenClaw framework — from installation to running autonomous email agents that keep your data private.",
    "content": "<h2>Why Run Your Email AI Locally?</h2>\n\n<p>Most AI-powered email tools work by routing your messages through third-party servers. Your prospects, your pitches, your follow-up cadences — all of it passing through infrastructure you don't control. For founders, sales teams, and developers who care about data hygiene, that's a real problem.</p>\n\n<p>MailClaw takes a different approach. It runs entirely on your Mac at <code>localhost:3100</code>, powered by <strong>OpenClaw</strong> — the open-source AI agent framework built to orchestrate autonomous, context-aware workflows without a cloud dependency. When you combine OpenClaw's agent runtime with MailClaw's email-specific tooling, you get a surprisingly capable pipeline that never phones home.</p>\n\n<p>This guide walks you through setting that pipeline up, from a fresh install to your first automated sequence running locally.</p>\n\n<h2>Understanding the OpenClaw Foundation</h2>\n\n<p>Before diving into commands, it's worth understanding what you're actually running. <strong>OpenClaw is an open-source AI agent framework</strong> designed for building task-oriented agents that can reason, use tools, and execute multi-step workflows. Think of it as the engine beneath the hood.</p>\n\n<p>MailClaw is built on top of OpenClaw, extending it with email-specific primitives: SMTP/OAuth connectors, contact enrichment pipelines, deliverability checks, and a sequence builder with branching logic. The separation matters because it means the agent behavior — how MailClaw decides when to follow up, how it drafts personalized copy, how it handles replies — is all driven by OpenClaw's agent runtime, which you can inspect, fork, and extend if you need to.</p>\n\n<p>For teams with custom requirements, this architecture is genuinely useful. You're not locked into a black box.</p>\n\n<h2>Getting Started: Install in 60 Seconds</h2>\n\n<p>You'll need Node.js 18+ on your Mac. Then run:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>That's it. The first run pulls the MailClaw package and its OpenClaw dependencies, initializes a local SQLite store for your contacts and sequences, and starts the dashboard server. You'll see something like:</p>\n\n<pre><code>✔ OpenClaw agent runtime initialized\n✔ MailClaw v1.x.x started\n→ Dashboard running at http://localhost:3100</code></pre>\n\n<p>Open <code>http://localhost:3100</code> in your browser and you'll land on the main dashboard. Everything from here — account setup, sequence building, agent configuration — happens through this local UI or via the CLI.</p>\n\n<h3>Connecting Your Email Accounts</h3>\n\n<p>MailClaw supports both SMTP credentials and OAuth flows (Gmail, Outlook). Go to <strong>Settings → Accounts</strong> and add your first sender. For Gmail, the OAuth path is the smoothest:</p>\n\n<ol>\n  <li>Click <em>Add Account → Google OAuth</em></li>\n  <li>Authorize MailClaw in the browser pop-up</li>\n  <li>Your refresh token is stored locally — never transmitted externally</li>\n</ol>\n\n<p>For SMTP setups, you'll provide your host, port, and credentials. MailClaw's deliverability monitor will immediately run a background check — verifying SPF, DKIM, and DMARC alignment so you know your sending infrastructure is solid before you send a single message.</p>\n\n<p>Multi-account support means you can manage separate sender identities from one dashboard — useful if you're running outreach across different domains or client accounts.</p>\n\n<h2>Building Your First AI Email Sequence</h2>\n\n<p>Head to <strong>Sequences → New Sequence</strong>. The sequence builder gives you a visual canvas for constructing multi-step email flows with conditional branching. But the real power is the AI step type, which hands control to an OpenClaw agent at runtime.</p>\n\n<h3>Step 1: Define the Goal</h3>\n\n<p>Start by writing a plain-English objective for your agent. For example:</p>\n\n<blockquote>\n  <p>\"Send a warm introduction to cold prospects in the developer tools space. If they open but don't reply within 3 days, send a short follow-up referencing what they likely care about based on their job title and company. Stop the sequence on any reply.\"</p>\n</blockquote>\n\n<p>MailClaw passes this goal — along with contact data and prior email history — to the underlying OpenClaw agent, which uses it to reason about timing, tone, and content at each step.</p>\n\n<h3>Step 2: Add Contact Enrichment</h3>\n\n<p>Before the first email goes out, run a contact enrichment step. MailClaw's enrichment pipeline pulls in publicly available signals — job title, company size, industry, recent activity — and stores them locally against each contact record.</p>\n\n<p>The OpenClaw agent then has richer context when drafting. Instead of a generic opener, it might reference that a prospect recently joined a Series B startup, or that their LinkedIn title shifted from \"Engineer\" to \"Head of Engineering\" — details that make personalization feel genuine rather than template-driven.</p>\n\n<h3>Step 3: Configure the AI Drafting Step</h3>\n\n<p>In the sequence canvas, add an <em>AI Draft</em> step and configure it with:</p>\n\n<ul>\n  <li><strong>Tone:</strong> Concise / Warm / Direct (pick one)</li>\n  <li><strong>Length cap:</strong> 120 words recommended for cold outreach</li>\n  <li><strong>Personalization fields:</strong> <code>{{first_name}}</code>, <code>{{company}}</code>, <code>{{enriched.role}}</code></li>\n  <li><strong>Avoid topics:</strong> Any sensitive terms or competitor names you want excluded</li>\n</ul>\n\n<p>The OpenClaw agent drafts the email at send time — not at sequence creation time — which means it incorporates any new enrichment data collected since the contact was added. This is a meaningful difference from static template systems.</p>\n\n<h2>Running the Agent Pipeline via CLI</h2>\n\n<p>If you prefer to work outside the dashboard or want to integrate MailClaw into a broader automation script, the CLI gives you full control:</p>\n\n<pre><code># List active sequences\nnpx mailclaw sequences list\n\n# Trigger a sequence manually for a single contact\nnpx mailclaw send --sequence \"developer-outreach\" --contact hello@example.com\n\n# Check deliverability status for all connected accounts\nnpx mailclaw deliverability check\n\n# View agent logs (OpenClaw reasoning traces)\nnpx mailclaw logs --agent --tail 50</code></pre>\n\n<p>The <code>--agent</code> log flag is particularly useful during setup. It surfaces the OpenClaw reasoning trace — you can see exactly how the agent interpreted the goal, what enrichment data it used, and why it chose the draft it did. It's the kind of transparency you rarely get with hosted tools.</p>\n\n<h2>Deliverability: Don't Skip This</h2>\n\n<p>Automation only works if your emails actually land in inboxes. MailClaw's deliverability monitor runs continuously in the background and surfaces issues before they become problems.</p>\n\n<p>From the dashboard, the <strong>Deliverability tab</strong> shows:</p>\n\n<ul>\n  <li>SPF / DKIM / DMARC pass rates per sending account</li>\n  <li>Bounce and complaint rate trends</li>\n  <li>Send volume recommendations to avoid throttling</li>\n  <li>Warm-up progress if you're onboarding a new domain</li>\n</ul>\n\n<p>The OpenClaw agent layer respects these signals automatically. If your complaint rate spikes above a configurable threshold, the agent will pause outbound sends and surface an alert on your dashboard rather than blowing up your sender reputation.</p>\n\n<h2>Privacy Architecture: What \"Local-First\" Actually Means</h2>\n\n<p>It's worth being specific about what stays on your machine. When MailClaw runs:</p>\n\n<ul>\n  <li>All contact data is stored in a local SQLite database at <code>~/.mailclaw/db</code></li>\n  <li>OAuth tokens and SMTP credentials are encrypted at rest locally</li>\n  <li>The OpenClaw agent runtime processes all reasoning and drafting locally (when using a local model) or via your own API key to a provider of your choice</li>\n  <li>Sequence logic, enrichment data, and send history never touch MailClaw's servers</li>\n</ul>\n\n<p>If you're using an external LLM API key (OpenAI, Anthropic, etc.), the drafted content does pass through that provider's API — that's worth knowing. But the metadata, contact records, and behavioral signals stay local. For many compliance scenarios, that distinction matters significantly.</p>\n\n<h2>Extending MailClaw with OpenClaw Plugins</h2>\n\n<p>Because MailClaw is built on the open-source OpenClaw framework, you can extend agent behavior by writing custom OpenClaw tools. A tool is just a function the agent can call during its reasoning loop.</p>\n\n<p>For example, you could write a tool that queries your CRM's API and returns deal stage data, then configure your sequence agent to adjust email tone based on where a prospect sits in your funnel. Or a tool that checks a prospect's GitHub activity before deciding whether a technical deep-dive angle is worth taking.</p>\n\n<p>Custom tools live in <code>~/.mailclaw/tools/</code> as standard JavaScript modules. MailClaw auto-loads them on startup and makes them available to any agent in your sequences.</p>\n\n<h2>A Practical Starting Point</h2>\n\n<p>If you're new to this, here's the simplest path to a working pipeline:</p>\n\n<ol>\n  <li>Run <code>npx mailclaw</code> and connect one sending account</li>\n  <li>Import a small contact list (CSV works) via <strong>Contacts → Import</strong></li>\n  <li>Run enrichment on the list and review what comes back</li>\n  <li>Build a two-step sequence: an AI-drafted intro, followed by a short follow-up if no reply in 4 days</li>\n  <li>Check the deliverability tab before activating</li>\n  <li>Review the OpenClaw agent logs after the first batch sends</li>\n</ol>\n\n<p>The whole thing takes under an hour to set up, and what you end up with is a genuinely autonomous email pipeline that runs on your laptop, reasons about your contacts, and keeps your data where it belongs — with you.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "email"
    ],
    "readTime": 6,
    "points": 164,
    "commentCount": 37
  },
  {
    "slug": "how-to-use-openclaw-agent-hooks-to-trigger-mailclaw-sequences-automatically",
    "title": "How to Use OpenClaw Agent Hooks to Trigger MailClaw Sequences Automatically",
    "author": "Ahmad Saroya",
    "date": "2026-03-17",
    "summary": "Learn how to wire OpenClaw agent hooks directly into MailClaw's sequence builder so that real-world events — a new lead, a completed task, a CRM update — automatically fire the right email at the right time, all from your local machine.",
    "content": "<h2>What Are OpenClaw Agent Hooks?</h2>\n\n<p>MailClaw is built on top of <strong>OpenClaw</strong>, an open-source AI agent framework designed to let developers compose, chain, and extend autonomous agents without shipping data to third-party infrastructure. One of OpenClaw's most powerful primitives is its <em>hook system</em> — a lightweight event bus that lets any agent emit named events, and lets any other part of your stack listen and react to them.</p>\n\n<p>In practice, this means your MailClaw installation isn't a siloed email tool. It's a first-class OpenClaw agent that can both emit and consume hooks. When you understand how to use those hooks, you unlock a genuinely programmable email automation layer that runs entirely on your Mac, at <code>localhost:3100</code>, with your data never touching an external server.</p>\n\n<h2>Why This Matters for Email Automation</h2>\n\n<p>Most email platforms force you into their event model. You're limited to triggers they've pre-built — form submissions, tag changes, purchase events. OpenClaw's hook system inverts that. <em>You</em> define what constitutes a meaningful event, and MailClaw listens for it.</p>\n\n<p>Some real-world scenarios where this pays off immediately:</p>\n\n<ul>\n  <li>A lead-enrichment agent finishes scoring a contact and you want to drop them into a nurture sequence automatically</li>\n  <li>Your support agent marks a ticket as resolved and you want to trigger a follow-up satisfaction sequence</li>\n  <li>A scraping agent detects a competitor price change and you want to alert a segment of your list</li>\n  <li>A task-management agent closes a project milestone and you want to send a client update sequence</li>\n</ul>\n\n<p>None of these require you to write a webhook handler, configure a third-party integration, or give an external service access to your contacts. Everything runs through the OpenClaw agent runtime already powering MailClaw locally.</p>\n\n<h2>Setting Up the Foundation</h2>\n\n<h3>Install MailClaw</h3>\n\n<p>If you haven't already, get MailClaw running with a single command:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>This bootstraps the full local stack — dashboard, SMTP/OAuth integration layer, AI agent runtime, and the OpenClaw hook bus — at <code>localhost:3100</code>. Open the dashboard, connect your email accounts, and make sure at least one sequence is saved before you wire up hooks.</p>\n\n<h3>Understand the Hook Bus Architecture</h3>\n\n<p>OpenClaw's hook bus is a typed event emitter with persistence. Every hook has three parts:</p>\n\n<ol>\n  <li><strong>A namespace</strong> — which agent or subsystem owns the event (e.g., <code>enrichment</code>, <code>crm</code>, <code>custom</code>)</li>\n  <li><strong>An event name</strong> — what happened (e.g., <code>contact.scored</code>, <code>deal.closed</code>)</li>\n  <li><strong>A payload</strong> — structured data attached to the event, typically including a contact email and any relevant metadata</li>\n</ol>\n\n<p>MailClaw registers itself as a listener on the OpenClaw bus at startup. When a hook fires with a contact identifier in the payload, MailClaw can map that contact to a sequence and enroll them automatically.</p>\n\n<h2>Emitting a Hook from Your Own Agent</h2>\n\n<p>You don't need to write a full OpenClaw agent from scratch to emit hooks. The OpenClaw SDK exposes a minimal <code>emit</code> interface you can call from any Node.js script, a cron job, or an existing automation.</p>\n\n<pre><code>import { OpenClawBus } from '@openclaw/sdk';\n\nconst bus = new OpenClawBus({ endpoint: 'http://localhost:3100/hooks' });\n\nawait bus.emit('custom.lead.qualified', {\n  contact: {\n    email: 'alex@example.com',\n    firstName: 'Alex',\n    company: 'Acme Corp',\n    score: 87\n  },\n  meta: {\n    source: 'enrichment-agent',\n    timestamp: new Date().toISOString()\n  }\n});</code></pre>\n\n<p>That's the entire emitter. The <code>endpoint</code> points to your local MailClaw instance. The payload can include any fields you want — MailClaw's contact enrichment layer will merge recognized fields (email, name, company) into the contact record automatically, and any extra metadata gets stored as custom properties you can use for sequence personalization later.</p>\n\n<h2>Mapping Hooks to Sequences in MailClaw</h2>\n\n<p>With the hook firing, head to the MailClaw dashboard at <code>localhost:3100</code> and navigate to <strong>Sequences → Hook Triggers</strong>.</p>\n\n<h3>Creating a Hook Trigger</h3>\n\n<ol>\n  <li>Click <strong>New Hook Trigger</strong></li>\n  <li>Set the <strong>Hook Event</strong> field to the exact event name you're emitting — in the example above, <code>custom.lead.qualified</code></li>\n  <li>Choose the <strong>Sequence</strong> you want to enroll matched contacts into</li>\n  <li>Optionally set <strong>Enrollment Conditions</strong> — for example, only enroll if <code>payload.contact.score >= 80</code></li>\n  <li>Configure <strong>Deduplication</strong> — prevent a contact from enrolling in the same sequence more than once per rolling window</li>\n  <li>Save the trigger</li>\n</ol>\n\n<p>From this point forward, every time your agent emits <code>custom.lead.qualified</code> with a valid contact email, MailClaw will enroll that contact and begin the sequence — handling send timing, multi-account rotation, and deliverability rules automatically.</p>\n\n<h3>Using Payload Data in Sequence Steps</h3>\n\n<p>MailClaw's sequence builder supports <strong>dynamic variables</strong> sourced from the hook payload. In your email step's body, reference payload fields using double-brace syntax:</p>\n\n<pre><code>Hi {{contact.firstName}},\n\nI noticed your team at {{contact.company}} recently {{meta.triggerReason}}.\nI thought it might be worth a quick conversation about...</code></pre>\n\n<p>This makes hook-triggered sequences feel hand-crafted rather than automated, because each email is rendered with the exact context that triggered enrollment — not just generic contact fields pulled from a static list.</p>\n\n<h2>A Practical End-to-End Example</h2>\n\n<p>Let's walk through a realistic setup: an OpenClaw enrichment agent that scores inbound leads, and a MailClaw sequence that fires for high-intent prospects.</p>\n\n<h3>Step 1 — Run the Enrichment Agent</h3>\n\n<p>OpenClaw's open-source ecosystem includes a community enrichment agent that cross-references email addresses against public data sources. Install and run it locally:</p>\n\n<pre><code>npx @openclaw/agent-enrichment --watch --emit-on-score 75</code></pre>\n\n<p>The <code>--emit-on-score</code> flag tells the agent to emit a <code>enrichment.contact.scored</code> hook whenever a contact clears the threshold. The agent runs locally — no API keys for data brokers, no external calls beyond the sources you configure.</p>\n\n<h3>Step 2 — Create the Sequence in MailClaw</h3>\n\n<p>In the MailClaw sequence builder, create a three-step outbound sequence:</p>\n\n<ul>\n  <li><strong>Day 0</strong> — Personalized intro referencing the contact's company and pain point (pulled from enrichment payload)</li>\n  <li><strong>Day 3</strong> — Follow-up with a relevant resource or case study</li>\n  <li><strong>Day 7</strong> — Soft close asking for a 15-minute call</li>\n</ul>\n\n<p>Enable <strong>deliverability monitoring</strong> on the sequence so MailClaw tracks open rates, bounce patterns, and sending reputation across your connected accounts in real time.</p>\n\n<h3>Step 3 — Wire the Hook Trigger</h3>\n\n<p>Back in Hook Triggers, create a new trigger listening for <code>enrichment.contact.scored</code>, mapped to your new sequence, with an enrollment condition of <code>payload.score >= 75</code>. Save it.</p>\n\n<p>Now test the full loop:</p>\n\n<pre><code>npx @openclaw/agent-enrichment --test-emit --email test@yourdomain.com</code></pre>\n\n<p>Check the MailClaw dashboard — you should see the contact appear in the sequence queue within seconds, with the enrichment payload attached and the first email scheduled.</p>\n\n<h2>Advanced Patterns Worth Knowing</h2>\n\n<h3>Chaining Hooks Across Multiple Agents</h3>\n\n<p>Because OpenClaw's bus is shared across all agents running on the same endpoint, you can build chains. An enrichment agent emits a hook → MailClaw enrolls the contact and emits its own <code>mailclaw.sequence.enrolled</code> hook → a CRM sync agent picks that up and creates a deal record. All of this happens locally, with no external orchestration layer.</p>\n\n<h3>Conditional Multi-Account Routing</h3>\n\n<p>MailClaw's multi-account support integrates with hook payload conditions. You can configure a trigger to route sends through a specific connected account based on payload metadata — for example, routing enterprise leads through your <code>enterprise@</code> address and SMB leads through <code>hello@</code>. Set this in the trigger's <strong>Account Routing</strong> field using the same condition syntax.</p>\n\n<h3>Debugging Hooks Locally</h3>\n\n<p>The MailClaw dashboard includes a <strong>Hook Event Log</strong> under Settings → Developer. Every incoming hook is logged with its full payload, matched trigger (if any), and enrollment result. If a hook fires but doesn't enroll a contact, the log will tell you exactly why — mismatched event name, failed condition, duplicate suppression, or a missing email field in the payload.</p>\n\n<blockquote>\n  <p><strong>Tip:</strong> During development, tail the MailClaw log from the terminal with <code>npx mailclaw --log-level debug</code>. You'll see hook events and trigger evaluations in real time, which makes iteration much faster than refreshing the dashboard.</p>\n</blockquote>\n\n<h2>Keeping It Private by Default</h2>\n\n<p>It's worth pausing on what's happening architecturally here. Your enrichment agent, your OpenClaw hook bus, and your MailClaw instance are all running on <code>localhost</code>. The hook payloads — which can contain scores, behavioral signals, company intelligence, and personal contact data — never leave your machine. This isn't a privacy policy promise from a vendor; it's just how the network topology works.</p>\n\n<p>For teams handling sensitive prospect data or operating in regulated industries, this architecture removes an entire category of compliance risk. The open-source nature of OpenClaw means you can audit exactly what the agent runtime does with every event, and extend or restrict behavior without waiting on a vendor roadmap.</p>\n\n<h2>Next Steps</h2>\n\n<p>The hook system is one of the most underused parts of the OpenClaw and MailClaw stack because it requires a bit of initial wiring — but once it's running, it's the kind of automation that genuinely fades into the background and just works. Start with one hook, one trigger, one sequence. Verify the loop end-to-end. Then layer in enrichment conditions, multi-account routing, and chained agent events as your needs grow.</p>\n\n<p>The OpenClaw GitHub repository has a growing collection of community agents that already emit standardized hook events, so check there before writing a custom emitter — there's a good chance someone has already built the integration you need.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "sequences"
    ],
    "readTime": 7,
    "points": 395,
    "commentCount": 44
  },
  {
    "slug": "mailclaw-contact-enrichment-how-openclaw-agents-research-prospects-locally",
    "title": "MailClaw Contact Enrichment: How OpenClaw Agents Research Prospects Locally",
    "author": "Anna Liu",
    "date": "2026-03-17",
    "summary": "Learn how MailClaw uses OpenClaw AI agents to enrich your contact database locally — researching prospects, filling in missing data, and building richer outreach lists without ever sending your data to a third-party server.",
    "content": "<h2>What Is Contact Enrichment, and Why Does Privacy Matter?</h2>\n\n<p>Contact enrichment is the process of taking a bare email address — or a partially complete contact record — and filling in the gaps. Job title, company name, LinkedIn profile, industry, company size, recent news mentions. Done well, it transforms a cold list into a rich prospect database that powers genuinely personalized outreach.</p>\n\n<p>The problem? Most enrichment tools work by shipping your contact data to a cloud API. You paste in a CSV of email addresses and someone else's server runs the research. That's a meaningful privacy risk, especially if you're working with contacts in regulated industries, under NDAs, or simply in a jurisdiction where GDPR compliance is a real concern.</p>\n\n<p>MailClaw takes a different approach. Because it runs entirely on your machine at <code>localhost:3100</code>, and because it's built on <strong>OpenClaw</strong> — an open-source AI agent framework — the enrichment logic runs locally too. Your contact list never leaves your Mac.</p>\n\n<h2>How OpenClaw Agents Power Local Enrichment</h2>\n\n<p><a href=\"https://github.com/openclaw\">OpenClaw</a> is the open-source AI agent framework underneath MailClaw. It provides the primitives for building agents that can plan multi-step tasks, use tools, reflect on their own outputs, and coordinate with other agents. MailClaw's contact enrichment feature is built directly on top of these primitives.</p>\n\n<p>When you trigger enrichment on a contact, MailClaw spins up an OpenClaw agent with a research goal. That agent has access to a set of tools — web search, HTML scraping, LinkedIn public profile parsing, company data lookups — and it works through a structured plan to fill in the fields you care about. The agent's reasoning loop, memory, and tool calls all happen on your local machine.</p>\n\n<p>Because OpenClaw is open source, you can inspect exactly what that agent is doing. There's no black box. If you want to customize the enrichment workflow — add new data sources, change the priority of fields, or restrict what gets looked up — you can fork the agent definition and drop it into MailClaw's agent directory.</p>\n\n<h3>The Enrichment Agent's Research Loop</h3>\n\n<p>Here's a simplified view of what the enrichment agent does for a single contact:</p>\n\n<ol>\n  <li><strong>Parse the seed data.</strong> The agent starts with whatever you already have — typically an email address and maybe a name. It extracts the domain from the email to identify the company.</li>\n  <li><strong>Company research.</strong> It queries public sources to determine the company's industry, size, and location. It may scrape the company's <code>about</code> page or look up a Crunchbase-style public profile.</li>\n  <li><strong>Individual research.</strong> Using the name (if available) and the company context, it searches for the contact's current role, LinkedIn headline, and any recent public activity.</li>\n  <li><strong>Confidence scoring.</strong> OpenClaw agents can reflect on their own outputs. The enrichment agent assigns a confidence score to each field — so you know whether a job title was confirmed from two sources or inferred from a single result.</li>\n  <li><strong>Write back to MailClaw.</strong> Enriched fields are written into your local contact database, where they become available in sequence templates, merge tags, and deliverability scoring.</li>\n</ol>\n\n<h2>Getting Started: Running Enrichment in MailClaw</h2>\n\n<p>If you haven't installed MailClaw yet, start here:</p>\n\n<pre><code>npx mailclaw</code></pre>\n\n<p>This pulls the latest release and starts the local dashboard at <code>localhost:3100</code>. On first run, MailClaw walks you through connecting your email accounts via SMTP or OAuth and configuring your local AI model.</p>\n\n<h3>Enriching a Single Contact</h3>\n\n<p>Open the Contacts section in your dashboard, find a contact, and click <strong>Enrich with AI</strong>. The enrichment agent spins up in the background. You'll see a live activity feed in the sidebar showing which tools the OpenClaw agent is invoking in real time — a useful transparency feature that reflects OpenClaw's open, auditable design.</p>\n\n<p>Enrichment typically takes 15–45 seconds per contact depending on how much public data is available and which local model you're running.</p>\n\n<h3>Bulk Enrichment via CSV Import</h3>\n\n<p>For larger lists, import a CSV and run bulk enrichment:</p>\n\n<ol>\n  <li>Go to <strong>Contacts → Import</strong> and upload your CSV. At minimum, include an <code>email</code> column. Name, company, and role columns are optional but give the agent a head start.</li>\n  <li>After import, select the contacts you want to enrich — or click <strong>Select All</strong>.</li>\n  <li>Click <strong>Enrich Selected</strong>. MailClaw queues the enrichment jobs and runs them with concurrency controls so your machine doesn't get overwhelmed.</li>\n  <li>Grab a coffee. Check back in the <strong>Enrichment Queue</strong> panel to track progress.</li>\n</ol>\n\n<p>You can also trigger enrichment from the CLI if you prefer a scriptable workflow:</p>\n\n<pre><code>npx mailclaw contacts enrich --input prospects.csv --output enriched.csv</code></pre>\n\n<p>This runs the full OpenClaw enrichment pipeline and writes results back to a local CSV, which you can inspect before importing into the contact database.</p>\n\n<h2>What Fields Get Enriched?</h2>\n\n<p>Out of the box, the MailClaw enrichment agent targets the following fields:</p>\n\n<ul>\n  <li><strong>Full name</strong> — confirmed or inferred from public profiles</li>\n  <li><strong>Job title and seniority level</strong> — useful for segmentation and personalization</li>\n  <li><strong>Company name, industry, and employee count</strong></li>\n  <li><strong>LinkedIn profile URL</strong> — scraped from public search results</li>\n  <li><strong>Company website and tech stack hints</strong> — inferred from job postings and public data</li>\n  <li><strong>Recent news or announcements</strong> — useful as conversation openers in sequences</li>\n  <li><strong>Location (city, country)</strong> — for time-zone-aware send scheduling</li>\n</ul>\n\n<p>Each field is stored alongside a <code>confidence</code> value (<em>high</em>, <em>medium</em>, or <em>low</em>) and a <code>source</code> note. This makes it easy to filter your list — for example, running a sequence only on contacts where job title confidence is <em>high</em>.</p>\n\n<h2>Using Enriched Data in Sequences</h2>\n\n<p>Enriched contact fields become merge tags in MailClaw's sequence builder. Once your contacts are enriched, you can write templates like:</p>\n\n<pre><code>Hi {{first_name}},\n\nI noticed {{company_name}} recently {{recent_news_snippet}} — congrats on that.\n\nI work with {{industry}} companies at the {{seniority}} level on...</code></pre>\n\n<p>The AI sequence builder (also powered by OpenClaw agents) can suggest personalizations based on enriched fields, turning a generic cold email into something that reads like you actually did your homework — because the agent did.</p>\n\n<blockquote>\n  <p><strong>Pro tip:</strong> Filter your sequence enrollment to contacts with a job title confidence of <em>high</em> or <em>medium</em> before you send. Low-confidence records are worth a manual review before they receive personalized outreach — a wrong job title in an opener does more damage than no personalization at all.</p>\n</blockquote>\n\n<h2>Customizing the Enrichment Agent</h2>\n\n<p>Because MailClaw is built on OpenClaw — and OpenClaw is open source — you're not stuck with the default enrichment behavior. The agent definitions live in your local MailClaw data directory:</p>\n\n<pre><code>~/.mailclaw/agents/enrichment/</code></pre>\n\n<p>You'll find YAML-based agent configs there. You can edit the tool list, change the system prompt, add a custom data source (an internal CRM export, for example), or adjust the confidence scoring thresholds. Changes are picked up on the next enrichment run — no restart required.</p>\n\n<p>If you build something useful, the OpenClaw community is a natural place to share it. Because the agent primitives are framework-level and not MailClaw-specific, enrichment agents built for MailClaw can often be adapted for other OpenClaw-powered tools.</p>\n\n<h2>A Note on Local Models and Enrichment Quality</h2>\n\n<p>Enrichment quality is partly a function of which local model you're running. MailClaw works with any OpenClaw-compatible local model — Ollama-served models like <code>llama3</code>, <code>mistral</code>, and <code>qwen2.5</code> all work well for enrichment tasks.</p>\n\n<p>For best results with contact research:</p>\n\n<ul>\n  <li>Use a model with at least 7B parameters for reliable entity extraction and multi-step reasoning.</li>\n  <li>Models with strong tool-use fine-tuning (like <code>mistral-nemo</code> or <code>qwen2.5:14b</code>) tend to produce more consistent structured output from scraped HTML.</li>\n  <li>If enrichment confidence scores are consistently low, try switching models in <strong>Settings → AI Model</strong> and re-running on a sample of contacts.</li>\n</ul>\n\n<p>The local-first architecture means you're trading some raw API throughput for complete data ownership. For most outreach volumes — hundreds to low thousands of contacts — the tradeoff is well worth it.</p>\n\n<h2>The Bottom Line</h2>\n\n<p>Contact enrichment has always been a tension between data quality and data privacy. MailClaw, built on the OpenClaw agent framework, resolves that tension by doing the research work locally. You get rich, personalized contact data; your prospects' information stays on your machine; and because OpenClaw is open source, you can audit, extend, and customize every step of the process.</p>\n\n<p>If you're building outreach sequences and working from thin contact lists, local enrichment is one of the highest-leverage features in MailClaw's toolkit. Run <code>npx mailclaw</code>, import your list, and let the agent do the legwork.</p>",
    "tags": [
      "mailclaw",
      "openclaw",
      "enrichment"
    ],
    "readTime": 7,
    "points": 256,
    "commentCount": 71
  },
];
